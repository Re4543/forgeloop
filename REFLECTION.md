# REFLECTION.md — ForgeLoop 项目反思

## 1. 项目概述

ForgeLoop 是一个自研的 Coding Agent Harness，负责把 LLM 的"下一步做什么"变成能真的读写代码、跑测试、出错了自己改的流程。六个维度都做了最低可运行实现，护栏那部分是主要投入。技术栈是 Python 3.12、SQLite 开 WAL、FastAPI，前端就一个 HTML 文件，没用任何 agent SDK。Docker 分发，GitLab CI 和 GitHub Actions 各配了一份。最后 51 个 commit，1978 行代码，1912 行测试，147 passed。

## 2. 设计决策回顾

最花时间想的是动作协议要不要用 function-calling。用了肯定省事，供应商帮你把 schema 校验做好，返回的就是结构化对象。但想了两天还是没用。原因是这门课要求交的就是 harness 这一层，而解析和校验正是 harness 该干的活，交给供应商等于把作业的核心部分外包了。更实际的问题是测试：用 function-calling 我没法测"LLM 吐了一段带 markdown 围栏的半截 JSON 会怎样"，因为那种情况根本到不了我的代码。所以自己定了个 JSON-in-text 协议，LLM 输出里带 `thought`、`tool`、`args`，解析器从自由文本里抠 JSON 出来，抠不到就把错误信息塞回上下文让它重来，连着失败 3 次熔断。代价是 parser 模块写了三级降级逻辑，比预想的麻烦。但换来的是 mock LLM 可以构造任意畸形输出，那部分测试写得很痛快。

审批阻塞用 DB 轮询还是 WebSocket，纠结的时间反而不长。WebSocket 明显更"现代"，延迟低。但审批状态本来就得落库，因为要求是进程重启后等待中的审批不能丢。既然状态已经在 SQLite 里了，加个 2 秒轮询几乎不花什么力气，而 WebSocket 要额外处理连接管理、断线重连、心跳，还得考虑内存状态和 DB 状态怎么同步。人点一下批准这种场景，2 秒延迟根本感觉不出来。这个决定现在看是对的。

多会话并发直接砍了。真要做得处理并发写 SQLite、会话之间的工作区隔离、资源冲突，任何一项都够再开一个 Plan。课程要的是机制跑通不是生产级并发。不过砍的时候留了个尾巴：我把 `PAUSED` 状态写进了状态机枚举，想着以后好扩展，但根本没写暂停和恢复的端点。冷启动验证的时候这个悬空状态被逮出来了。留接口不实现这种事，看起来是"有远见"，实际上是给文档埋雷。

## 3. Subagent 驱动开发体验

34 个 task，每个派一个新的 subagent 做，做完之间穿插 review。

上下文隔离这一点是真有用。每个 subagent 只看得到自己那个 brief，前面 30 个 task 干了什么它一概不知。这反过来逼我把模块边界划清楚——brief 写不明白，说明这个模块的职责本身就没想清楚。也不会出现长会话里那种情况，做到第 20 个 task 的时候 agent 还在纠结第 3 个 task 留下的某个命名。

review 的收获超出我预期。Plan 1 的 reviewer 挑出 4 个 Important 问题，都是实现的人自己看不见的：Deny 分支有个 `NameError`（那条错误路径测试根本没覆盖到）、feedback 去重的逻辑、轮次计数差一、还有 `done` 动作被拒绝时没处理。这几个都是真 bug 不是风格问题。让一个对代码没有感情的 agent 来读，确实比自查管用。

最难受的是 brief 里的代码跟框架不兼容。Plan 2 的 Task 5 我连修了三次。问题出在我在 brief 里写了 FastAPI 的示例代码，凭印象写的：`Depends` 的嵌套用法记错了，`HTTPException(detail={...})` 序列化出来是 `{"detail": {...}}`，而 spec 要求扁平的 `{"error": "msg"}`。subagent 老老实实照着抄，然后测试全挂。这事让我改了写 brief 的习惯：不要写看起来能跑的代码，写意图和契约就够了。写伪代码或者直接写断言，都比写半吊子真代码安全，因为半吊子代码会被当成权威照抄。

另一个坑是 stub 到实现的过渡。`_await_approval` 在 Plan 1 里就是一句 `pass`，所有相关测试都默认"审批自动通过"。Plan 2 改成真轮询以后控制流变了，一片测试跟着塌。后来的做法是 stub 必须写注释说明"这是有意留的空实现，Plan X 会补"，让 reviewer 和后面的 subagent 都能看到。

数字上：34 个 task，147 个测试，4 个专门修 bug 的 subagent。这 4 个全在 Plan 2。Plan 1 那 25 个 task 只需要 3 次人工干预，Plan 2 才 9 个 task 就要 4 个 fix subagent。区别在于 Plan 1 基本是纯标准库，Plan 2 引入了 FastAPI。写自包含逻辑 subagent 很稳，一碰第三方框架就容易出事。

## 4. 冷启动验证教训

冷启动验证的意思是拉一个全新的 agent 来，只给它 SPEC.md 和 PLAN.md，看它能不能照着实现出东西。我这里有个流程错误：顺序做反了。

应该是先写 spec、再冷启动验证、最后实现。我实际是写完 spec 就开始实现，全做完了才想起来验证。结果那个 agent 打开 PLAN.md，看到 34 个 task 全打了勾，没有一个能做的。这次验证就只剩下"看看 spec 写得清不清楚"这个作用了，验证不了可实现性。而且这个错误补不回来，代码一旦写完，就再没有"假装不知道实现"的机会。

不过退化版的验证还是抓到了 5 个问题，有两个挺典型。

一个是 spec 和代码对不上。SPEC.md 第 740 行写审批超时后状态叫 `EXPIRED`，`sweeper.py:43` 实际写的是 `TIMEOUT`。写 spec 时随手用了一个词，实现时用了另一个，各自都自洽，只有把两个文件并排看才发现。我自己 review 的时候眼睛会自动把它们当成一个东西，全新的 agent 不会。

另一个是 spec 吹过头了。R4 那条风险的缓解措施里我列了"限速"，代码里压根没有 rate limiting。这不是漏实现，是写 spec 的时候把"应该有"和"会有"混在一起写了。改法是把那行改成"文档推荐反代+TLS+限速，v1 未实现限速，列为已知限制"。写清楚哪里没做，比装作全做了有用。

还有个问题是 PLAN.md 一开始只是个索引，指向 `docs/superpowers/plans/` 下面的详细文件，本身不含 task 定义。人看索引没问题，冷启动 agent 就断链了。给全新 agent 的入口文档得自包含。

## 5. AI 协作反思

委托出去效果好的是那种契约清楚的活。工具层那 6 个 tool（read_file、write_file、list_dir、run_shell、run_tests、done），输入输出定义死了，subagent 基本零干预就写完了。测试代码也适合，尤其是边界情况的穷举，它比我有耐心得多。CI 配置、Dockerfile、README 这类东西也还行，因为"什么算对"有公认标准，不需要项目独有的判断。

不适合委托的是要做取舍的地方。就说 function-calling 那个问题，AI 能把两边优缺点列得很齐，但列不出"这门课考的就是这一层，外包掉等于交白卷"这条——那个理由压根不在代码里，在课程语境里。推迟 WebSocket 也一样，依据是我手上还剩多少时间和精力，AI 无从知道。spec 也不适合完全交出去。spec 是意图的载体，意图本身没想清楚的话，AI 只会把模糊放大成看起来很正式的模糊，我那条"限速"就是这么来的。

反思这种东西更不能交。让它写这份文档，它能写出结构工整的段落，但写不出 Task 5 修到第三遍时我盯着那个 `{"detail": ...}` 突然明白 brief 里不该塞真代码的那一下。

大致的边界我觉得是这样：我想清楚了的地方，可以让它替我执行；没想清楚的地方，指望它替我想通，只会得到一份包装得很好的糊涂账。它的产出质量上限就是输入的清晰度。brief 写得清楚，出来的东西接近完美；brief 里有含糊，含糊会被原样放大，而且因为产出看着很专业，那点含糊更难被发现。所以真正的重点不在怎么让 AI 写得更好，在于怎么逼自己把要求说清楚。这个项目 34 个 brief，每写一个都是在把脑子里那团模糊的想法翻译成可验证的契约，这个翻译过程本身就是设计工作。

## 6. 总结

收获最大的一点是搞明白了什么该自己写、什么可以交出去。function-calling、agent SDK、现成的 WebSocket 方案，每样都能让我少写不少代码，但少写的恰好是我需要弄懂和验证的那部分。AI 协作也是同一件事：subagent 帮我写了 1978 行代码，但那 34 个 brief——把模糊需求变成可验证契约的过程——躲不掉，只能自己一个一个走。
